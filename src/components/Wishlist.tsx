import React, { ReactElement } from "react";

interface Props {}

function Wishlist({}: Props): ReactElement {
  return (
    <div>
      <h1>Wishlist</h1>
    </div>
  );
}

export default Wishlist;
